# 2026-08-09

## 修复两个安全/正确性问题（前次评估发现的 🔴 缺陷）
- **路径遍历漏洞** `internal/server/server.go`：approve/reject 处理器原直接用用户可控的 `?path=` 调 `fs.ParseFile`/`os.Remove`。已加 `filepath.Clean` + `filepath.Rel` 守卫，把目标锁在 `Store.DraftsDir()` 内，越界一律拒回 400。
- **score 恒为 0** `internal/server/server.go`：原读 `Document.Score`（从未被赋值）。改为读 `search.Result.Score`（命中次数），并对调用方转为正分（`Score = -hitCount` 是内部排序技巧，对外应 higher=better）。
- 删除了 `fs.Document.Score` 死字段（bug 根源）。
- 新增 `internal/server/server_test.go`：含真实利用尝试（绝对路径 / `../` 遍历删 victim.txt 被拦）+ 合法 approve 流程不被破坏。
- 校验：`go build ./...` / `go test ./...` / `go vet ./...` 全绿；端到端实测 score 返回真实值、两次攻击均被拦、合法草稿审核正常。
- 清理：删除了 e2e 测试目录（曾被 safe-delete 拦截，杀掉残留 kbserver 进程后 `find -delete` 成功）。

## 文档
- `docs/next.md` → 改名为 `docs/design-principles.md`（设计原则 + MVP 规划 + 参考来源总纲，与 `architecture.md` 技术视图互补）。
- 在 `docs/kb-pilot-evaluation.md` 补正："完全无参考来源"判断需修正——`design-principles.md` 已含 LLM-Wiki / MemSkill 等 prior art，但未被 `architecture.md` 引用，仍缺正式入口。

## 备注
- 仓库存在**历史遗留 gofmt 漂移**（gofmt -l 列 8 个文件，含未改动的 search/tag/watch/main 等，Go 1.26.3）。本次只 gofmt 了我改动的两个文件，未动无关文件以免产生噪音 diff。若要全仓统一格式需单独一次 `gofmt -w` 提交。

## 全仓 gofmt + 文档整理（后续）
- **全仓 gofmt**：执行 `gofmt -w internal cmd`，`gofmt -l` 已无漂移，`build/test/vet` 全绿。此前 8 个文件的漂移已统一（改动落在 main.go / fs.go / search.go / search_test.go / server.go / tag.go / tag_test.go / watch.go）。
- **文档分类整理**（用户提出"文档太多"）：
  - 新增 `docs/README.md` 文档地图（定位 + 阅读顺序）。
  - `docs/mvp-evaluation.md` → `docs/decisions/2026-06-28-mvp-evaluation.md`
  - `docs/kb-pilot-evaluation.md` → `docs/decisions/2026-08-08-kb-pilot-evaluation.md`
  - `docs/waylondev-kb-pilot.md` → `docs/references/kb-pilot-source.md`（外部素材归参考区）
  - 在决策文档末尾新增"附：来源文章核心论点"小节，把外部文章论点整合进决策文档，使其自成一体（响应用户"精简整理到决策内部"的诉求）。
  - `docs/archive/` 为空目录（知识归档在 `../knowledge-repo`），与文档无关。
  - 注：`waylondev-kb-pilot-backup.md` 经核查当前工作区已不存在（之前提到的重复副本已消失）。

## 清理 kb-pilot-source.md（用户要求不留在项目里）
- 删除 `docs/references/kb-pilot-source.md`（外部文章全文；从未纳入 git 跟踪，删除即从工作区移除）。理由：决策已定为"不采纳 kb-pilot 作为下一形态"，原文不应占用项目文档位；其核心论点此前已整合进 `decisions/2026-08-08-kb-pilot-evaluation.md` 文末，故无信息损失。
- 同步清理已空的 `docs/references/` 目录。
- 修正引用死链：决策文档 3 处 `docs/references/kb-pilot-source.md` 引用改为指向原文仓库 https://github.com/waylondev/kb-pilot ；`docs/README.md` 文档地图的 `references/` 分类段改为一条指向原文仓库的说明，并将"三类"改为"两类"。
- 成果：`docs/` 仅剩核心文档 3 份（README.md / architecture.md / design-principles.md）+ `decisions/` 2 份评估；全仓已无对 source 文件的残留引用。
- 删除用 PowerShell `Remove-Item` 执行（bash `rm` 被 safe-delete wrapper 因 `/d/` 路径格式 fail-closed 拦截，非个人目录、已获用户授权，安全）。

## 核对"当前最紧要任务"并对照主流 (本次会话)
- 用户问：当前最紧要开发任务？方向是否符合主流？
- 关键纠正：决策文档 `2026-08-08-kb-pilot-evaluation.md` 第11节称两个 🔴 缺陷"均未修复"——**已过时**。提交 `00083d1` 已修复路径遍历 + score 恒为0，且 `internal/server/server_test.go` 已加 HTTP 层回归测试。`go build/test/vet` 全绿。
- 仍存的真实缺口：
  - `internal/fs` 无任何测试（ParseFile/WriteDraft/ApproveDraft/RejectDraft 文件移动核心逻辑无覆盖），安全修复的 fs 层回归测试缺位——最该补。
  - 🟡 缺陷仍在：`main.go` 双重 BuildIndex（#3）、`search.go` 内容仅索引前100字（#4，召回硬伤）、`tag.go` 维度前缀过宽（#6）。
  - 治理债：`architecture.md` 仍未加"设计参考与取舍"一节，`design-principles.md` 仍未被其/README 引用。
- 主流对照（WebSearch 确认）：2025-2026 行业收敛于"file-based memory 是 agent state 默认"——Anthropic Memory 工具(文件系统, 2025-08)、Context Engineering、Linux Foundation Agentic AI Foundation(AGENTS.md)、Karpathy LLM Wiki gist(2026-04)。本项目"文件系统即数据库 + 结构化 tag + 人在回路"与之高度吻合且独立收敛。拒绝 kb-pilot、保留 PageIndex 作备选，与 2026 "vectorless RAG" 浪潮一致（PageIndex ~3万 star，仅适用长文档，正契合本项目"原子条目已绕开切块"的判断）。平衡点：向量 RAG 在"大规模非结构化语料/模糊语义检索"仍最优；P1 的 MinerU 批量导入推高体量后，项目规划的"summary 路由层"恰是"outgrow 文件后加一层"的正确过渡。

## 落档与修正过时文档（用户要求）
- 创建 `docs/decisions/2026-08-09-status-check.md`：状态核对与主流方向对照（纠正决策文档第11节过时结论、当前代码真实状态核查表、仍存缺口 P0-P2、WebSearch 主流对照）。
- 修正过时文档：`2026-08-08-kb-pilot-evaluation.md` 第十一节加"补正(2026-08-09)"标注，原"两缺陷未修复/本末倒置"结论标记过时；`2026-06-28-mvp-evaluation.md` 顶部加"补正"标注两项 🔴 已修复；`docs/README.md` 决策表新增状态核对文档行并标注两项 🔴 已修复、追加勿视作阻塞项的提示。

## 修复 P0/P1（用户要求，code-purity 准则）
- **P0 fs 测试**：新增 `internal/fs/fs_test.go`（6 个测试：ParseFile 表格 4 例、WriteDraft、Reload 排除非 active、ApproveDraft 按域归类/默认 common、RejectDraft）。注意：WriteDraft 不建 drafts 目录，测试需先 MkdirAll；tag 含 `:` 时 yamlString 会加引号，断言用子串匹配。
- **P1 #4 内容索引上限**：`internal/search/search.go` 去掉前 100 字截断，改为索引全文。
- **P1 #3 双重 BuildIndex**：`cmd/server/main.go` watcher 回调简化为 `return store.Reload()`（Reload 内部触发 onReload 回调统一 BuildIndex），净删 5 行。
- **P1 文档治理债**：`docs/architecture.md` 末尾新增"📚 设计参考与取舍"节（7 行表），打通与 `design-principles.md` 的引用链。
- 验证：`go build/vet/test ./...` 全绿，gofmt 无漂移，diff 最小。
